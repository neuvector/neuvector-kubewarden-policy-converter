apiVersion: batch/v1
kind: CronJob
metadata:
  name: test-cronjob-run-as-root-privileged
  namespace: default
spec:
  schedule: "*/15 * * * *"
  jobTemplate:
    spec:
      template:
        spec:
          containers:
          - name: privileged-task
            image: busybox:1.35
            command:
            - /bin/sh
            - -c
            - "echo 'Running privileged task as root'; whoami; sleep 10"
            securityContext:
              privileged: true
            resources:
              requests:
                memory: "16Mi"
                cpu: "25m"
          restartPolicy: Never
