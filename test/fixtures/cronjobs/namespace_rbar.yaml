apiVersion: batch/v1
kind: CronJob
metadata:
  name: test-cronjob-namespace-rbar
  namespace: rbar
spec:
  schedule: "0 2 * * *"
  jobTemplate:
    spec:
      template:
        spec:
          containers:
          - name: test-container
            image: gcr.io/distroless/base:nonroot
            command:
            - /bin/sh
            - -c
            - echo "Hello from CronJob in namespace rbar"
          restartPolicy: OnFailure
