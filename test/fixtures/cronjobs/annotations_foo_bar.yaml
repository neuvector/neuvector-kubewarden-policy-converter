apiVersion: batch/v1
kind: CronJob
metadata:
  name: annotations-foo-bar-cronjob
  namespace: default
  annotations:
    foo: "1"
    bar: "2"
spec:
  schedule: "0 */6 * * *"  # Every 6 hours
  jobTemplate:
    spec:
      template:
        metadata:
          name: annotations-foo-bar-job
          annotations:
            foo: "1"
            bar: "2"
        spec:
          containers:
            - name: nginx
              image: nginx:latest
              command: ["nginx", "-g", "daemon off;"]
              resources:
                requests:
                  memory: "16Mi"
                  cpu: "25m"
          restartPolicy: OnFailure
