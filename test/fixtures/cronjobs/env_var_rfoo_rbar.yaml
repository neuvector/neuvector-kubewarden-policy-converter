apiVersion: batch/v1
kind: CronJob
metadata:
  name: rfoo-rbar-cronjob
spec:
  schedule: "0 */6 * * *"  # Every 6 hours
  jobTemplate:
    spec:
      template:
        metadata:
          name: rfoo-rbar-job
        spec:
          containers:
            - name: nginx
              image: nginx:latest
              command: ["nginx", "-g", "daemon off;"]
              env:
                - name: rfoo
                  value: "1"
                - name: rbar
                  value: "2"
          restartPolicy: OnFailure
