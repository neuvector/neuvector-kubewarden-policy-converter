apiVersion: apps/v1
kind: Deployment
metadata:
  name: test-deployment-privileged
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: test-app-priv
  template:
    metadata:
      labels:
        app: test-app-priv
    spec:
      containers:
      - name: test-container
        image: nginx:latest
        securityContext:
          allowPrivilegeEscalation: true
        resources:
          requests:
            memory: "64Mi"
            cpu: "250m"
