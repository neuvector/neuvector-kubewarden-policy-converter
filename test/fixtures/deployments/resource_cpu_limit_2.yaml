apiVersion: apps/v1
kind: Deployment
metadata:
  name: resource-limit-cpu-too-high
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: resource-limit-cpu-too-high
  template:
    metadata:
      labels:
        app: resource-limit-cpu-too-high
    spec:
      containers:
      - name: app
        image: nginx:1.25
        resources:
          limits:
            cpu: "2"


