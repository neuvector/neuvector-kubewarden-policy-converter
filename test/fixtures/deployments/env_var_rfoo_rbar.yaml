apiVersion: apps/v1
kind: Deployment
metadata:
  name: rfoo-rbar-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      name: rfoo-rbar-pod
  template:
    metadata:
      labels:
        name: rfoo-rbar-pod
    spec:
      containers:
        - name: nginx
          image: nginx:latest
          env:
            - name: rfoo
              value: "1"
            - name: rbar
              value: "2"
