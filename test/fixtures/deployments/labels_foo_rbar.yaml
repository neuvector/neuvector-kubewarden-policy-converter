apiVersion: apps/v1
kind: Deployment
metadata:
  name: labels-foo-rbar-deployment
  namespace: default
  labels:
    foo: "1"
    rbar: "3"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: labels-foo-rbar-app
  template:
    metadata:
      labels:
        app: labels-foo-rbar-app
        foo: "1"
        rbar: "3"
    spec:
      containers:
      - name: app-container
        image: nginx:latest
        resources:
          requests:
            memory: "64Mi"
            cpu: "250m"
