apiVersion: apps/v1
kind: Deployment
metadata:
  name: annotations-foo-rbar-deployment
  namespace: default
  annotations:
    foo: "1"
    rbar: "3"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: annotations-foo-rbar-app
  template:
    metadata:
      labels:
        app: annotations-foo-rbar-app
      annotations:
        foo: "1"
        rbar: "3"
    spec:
      containers:
      - name: app-container
        image: nginx:latest
        resources:
          requests:
            memory: "64Mi"
            cpu: "250m"
