apiVersion: v1
kind: Pod
metadata:
  name: test-pod-namespace-rbar
  namespace: rbar
spec:
  containers:
  - name: web-server
    image: gcr.io/distroless/base-nonroot
    resources:
      requests:
        memory: "128Mi"
        cpu: "100m"
      limits:
        memory: "256Mi"
        cpu: "500m"
